# Deprecated

This directory is no longer used. InfluxDB is started from the official `influxdb:2.1.1` image in `compose.yaml`, initialized via `DOCKER_INFLUXDB_INIT_*` variables in `.env`.

The custom `dockerfile` and `setup_influx.sh` are kept for reference only.
